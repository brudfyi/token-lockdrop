import React from 'react'
import {Typography} from '../../components'

const DisconnectedContent = () => {
  return <Typography>Please connect a wallet to lock FLOW</Typography>
}

export default DisconnectedContent
